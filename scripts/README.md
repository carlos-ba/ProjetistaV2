# Scripts

Scripts utilitarios para padronizar o ambiente entre desktop e notebook.

## PowerShell (Windows)

Setup inicial do backend:

```powershell
.\scripts\setup_backend.ps1
```

Subir API local:

```powershell
.\scripts\run_backend.ps1
```
